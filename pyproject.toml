[project]
name = "reader"
version = "0.1.0"
description = "Reader: a personal reading hub for one person."
requires-python = ">=3.12"
dependencies = [
    "fastapi==0.116.1",
    "uvicorn[standard]==0.35.0",
    "jinja2==3.1.6",
    "python-multipart==0.0.20",
    "httpx==0.28.1",
    "feedparser==6.0.11",
    "trafilatura==2.2.0",
    "pytest==8.4.1",
]

[tool.uv]
package = false
